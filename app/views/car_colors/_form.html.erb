<%= form_for(@car_color) do |f| %>
  <% if @car_color.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@car_color.errors.count, "error") %> prohibited this car_color from being saved:</h2>

      <ul>
      <% @car_color.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :color %><br />
    <%= f.text_field :color %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
